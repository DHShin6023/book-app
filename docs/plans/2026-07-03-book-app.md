# 도서 추천 앱 (책갈피 AI) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 단어 3~10개를 입력하면 OpenRouter AI가 실존 도서 3~5권을 추천하고 Google Books로 표지를 붙여 보여주는 정적 PWA.

**Architecture:** 프레임워크 없는 순수 HTML/CSS/JS 단일 페이지 앱. 순수 로직(단어 검증, JSON 파싱)은 `logic.js`로 분리해 Node로 테스트하고, UI/네트워크는 `app.js`에서 처리. 칼로리앱(`~/Desktop/ClaudeWork/calorie-app/`)과 동일한 뷰 전환/설정 패턴.

**Tech Stack:** Vanilla JS, OpenRouter chat/completions API, Google Books Volumes API(키 불필요), PWA(manifest+service worker), GitHub Pages 배포.

## Global Constraints

- 프로젝트 위치: `/Users/shin/Desktop/ClaudeWork/book-app/` (git 저장소 이미 초기화됨, 설계 문서 커밋 존재)
- 모든 UI 문구는 한국어
- 외부 라이브러리/빌드 도구 금지 — 파일을 그대로 GitHub Pages에 올려서 동작해야 함
- API 키는 localStorage에만 저장, 코드에 하드코딩 금지
- 단어 개수 규칙: 최소 3개(미만이면 추천 버튼 비활성), 최대 10개(도달 시 추가 차단)
- 추천 결과: 최소 3권, 최대 5권 표시
- 순수 로직 테스트: `node tests/logic.test.js` (테스트 프레임워크 없이 `assert` 사용)
- UI 검증: `python3 -m http.server 8080` 후 브라우저에서 수동 확인

---

### Task 1: 프로젝트 뼈대 + PWA 셸

**Files:**
- Create: `index.html`, `style.css`, `manifest.json`, `icons/icon-192.png`, `icons/icon-512.png`

**Interfaces:**
- Produces: `index.html`의 DOM id들 — 이후 태스크의 `app.js`가 의존함: `view-main`, `view-settings`, `input-word`, `btn-add-word`, `chips`, `word-counter`, `btn-recommend`, `loading`, `error-box`, `results`, `btn-settings-open`, `input-api-key`, `select-model`, `btn-save-settings`, `toast`

- [ ] **Step 1: index.html 작성**

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
  <title>책갈피 AI - 도서 추천</title>
  <meta name="theme-color" content="#5B4636">
  <link rel="stylesheet" href="style.css">
  <link rel="manifest" href="manifest.json">
  <link rel="apple-touch-icon" href="icons/icon-192.png">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="default">
</head>
<body>

  <!-- 메인 화면 -->
  <div id="view-main" class="view active">
    <header>
      <h1>📚 책갈피 AI</h1>
      <button id="btn-settings-open" class="icon-btn" aria-label="설정">⚙️</button>
    </header>
    <p class="subtitle">지금 마음에 드는 단어를 3~10개 입력하면<br>어울리는 책을 추천해드려요</p>

    <div class="word-input-row">
      <input id="input-word" type="text" maxlength="20" placeholder="단어 입력 (예: 여행)" autocomplete="off">
      <button id="btn-add-word">추가</button>
    </div>

    <div class="chip-area">
      <div id="chips"></div>
      <span id="word-counter">0/10</span>
    </div>

    <button id="btn-recommend" class="primary-btn" disabled>도서 추천 받기</button>

    <div id="loading" class="hidden">
      <div class="spinner"></div>
      <p>AI가 책을 고르는 중...</p>
    </div>

    <div id="error-box" class="hidden"></div>
    <div id="results"></div>
  </div>

  <!-- 설정 화면 -->
  <div id="view-settings" class="view">
    <header>
      <button class="back-btn" data-target="main" aria-label="뒤로">←</button>
      <h1>설정</h1>
    </header>
    <label for="input-api-key">OpenRouter API 키</label>
    <input id="input-api-key" type="password" placeholder="sk-or-v1-..." autocomplete="off">
    <label for="select-model">AI 모델</label>
    <select id="select-model"></select>
    <button id="btn-save-settings" class="primary-btn">저장</button>
    <p class="hint">API 키는 openrouter.ai/keys 에서 발급받을 수 있습니다.<br>키는 이 기기의 브라우저에만 저장되며 외부로 전송되지 않습니다 (OpenRouter 호출 제외).</p>
  </div>

  <div id="toast"></div>

  <script src="logic.js"></script>
  <script src="app.js"></script>
  <script>
    if ('serviceWorker' in navigator) {
      navigator.serviceWorker.register('sw.js');
    }
  </script>
</body>
</html>
```

- [ ] **Step 2: style.css 작성** (종이/책 느낌의 따뜻한 톤)

```css
* { margin: 0; padding: 0; box-sizing: border-box; }
:root {
  --brown: #5B4636;
  --brown-light: #8B7355;
  --cream: #FAF6F0;
  --card: #FFFFFF;
  --accent: #C0392B;
  --text: #3D3229;
  --muted: #9C8E7E;
}
body {
  font-family: -apple-system, BlinkMacSystemFont, 'Apple SD Gothic Neo', sans-serif;
  background: var(--cream);
  color: var(--text);
  min-height: 100vh;
  padding-bottom: env(safe-area-inset-bottom);
}
.view { display: none; max-width: 640px; margin: 0 auto; padding: 16px; }
.view.active { display: block; }
header { display: flex; align-items: center; justify-content: space-between; padding: 8px 0 16px; }
header h1 { font-size: 22px; color: var(--brown); }
.icon-btn, .back-btn { background: none; border: none; font-size: 22px; cursor: pointer; color: var(--brown); }
.subtitle { text-align: center; color: var(--muted); font-size: 14px; line-height: 1.6; margin-bottom: 20px; }

.word-input-row { display: flex; gap: 8px; margin-bottom: 12px; }
.word-input-row input {
  flex: 1; padding: 12px 14px; border: 1.5px solid #E3D9CC; border-radius: 12px;
  font-size: 16px; background: var(--card); outline: none;
}
.word-input-row input:focus { border-color: var(--brown-light); }
.word-input-row button {
  padding: 12px 18px; border: none; border-radius: 12px; background: var(--brown);
  color: #fff; font-size: 15px; font-weight: 600; cursor: pointer;
}
.word-input-row button:disabled { background: #CBBFAF; }

.chip-area { min-height: 48px; margin-bottom: 16px; }
#chips { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 6px; }
.chip {
  display: inline-flex; align-items: center; gap: 6px; padding: 7px 12px;
  background: #EFE6D8; border-radius: 999px; font-size: 14px; color: var(--brown);
}
.chip button { background: none; border: none; cursor: pointer; font-size: 14px; color: var(--brown-light); padding: 0; }
#word-counter { font-size: 12px; color: var(--muted); }

.primary-btn {
  width: 100%; padding: 15px; border: none; border-radius: 14px;
  background: var(--brown); color: #fff; font-size: 17px; font-weight: 700; cursor: pointer;
}
.primary-btn:disabled { background: #CBBFAF; cursor: default; }

.hidden { display: none !important; }
#loading { text-align: center; padding: 36px 0; color: var(--muted); }
.spinner {
  width: 36px; height: 36px; margin: 0 auto 12px;
  border: 4px solid #EFE6D8; border-top-color: var(--brown);
  border-radius: 50%; animation: spin 0.9s linear infinite;
}
@keyframes spin { to { transform: rotate(360deg); } }

#error-box {
  margin-top: 16px; padding: 14px; border-radius: 12px;
  background: #FDECEA; color: var(--accent); font-size: 14px; line-height: 1.5;
}

#results { margin-top: 20px; display: flex; flex-direction: column; gap: 14px; }
.book-card {
  display: flex; gap: 14px; padding: 14px; background: var(--card);
  border-radius: 16px; box-shadow: 0 2px 8px rgba(91,70,54,0.08);
}
.book-cover { width: 88px; flex-shrink: 0; }
.book-cover img { width: 88px; height: 128px; object-fit: cover; border-radius: 8px; }
.cover-placeholder {
  width: 88px; height: 128px; border-radius: 8px; padding: 8px;
  background: linear-gradient(160deg, var(--brown-light), var(--brown));
  color: #fff; font-size: 12px; font-weight: 600; line-height: 1.4;
  display: flex; align-items: center; justify-content: center; text-align: center;
  word-break: keep-all; overflow: hidden;
}
.book-info { flex: 1; min-width: 0; }
.book-info h3 { font-size: 16px; color: var(--brown); margin-bottom: 2px; }
.book-author { font-size: 13px; color: var(--muted); margin-bottom: 8px; }
.no-cover-badge {
  display: inline-block; font-size: 11px; color: var(--accent);
  background: #FDECEA; padding: 2px 8px; border-radius: 999px; margin-bottom: 6px;
}
.book-reason { font-size: 13px; line-height: 1.55; margin-bottom: 6px; }
.book-reason strong { color: var(--brown); }
.book-summary { font-size: 13px; line-height: 1.55; color: var(--muted); }

/* 설정 화면 */
#view-settings label { display: block; font-size: 14px; font-weight: 600; color: var(--brown); margin: 16px 0 6px; }
#view-settings input, #view-settings select {
  width: 100%; padding: 12px 14px; border: 1.5px solid #E3D9CC; border-radius: 12px;
  font-size: 15px; background: var(--card); outline: none;
}
#view-settings .primary-btn { margin-top: 24px; }
.hint { margin-top: 14px; font-size: 12px; color: var(--muted); line-height: 1.6; }

#toast {
  position: fixed; bottom: 32px; left: 50%; transform: translateX(-50%) translateY(20px);
  background: rgba(61,50,41,0.92); color: #fff; padding: 11px 20px; border-radius: 999px;
  font-size: 14px; opacity: 0; pointer-events: none; transition: all 0.25s; max-width: 85vw; text-align: center;
}
#toast.show { opacity: 1; transform: translateX(-50%) translateY(0); }
```

- [ ] **Step 3: manifest.json 작성**

```json
{
  "name": "책갈피 AI - 도서 추천",
  "short_name": "책갈피 AI",
  "start_url": ".",
  "display": "standalone",
  "background_color": "#FAF6F0",
  "theme_color": "#5B4636",
  "icons": [
    { "src": "icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "icons/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

- [ ] **Step 4: 아이콘 생성** (단색 PNG를 파이썬 표준 라이브러리로 생성)

Run (프로젝트 루트에서):
```bash
mkdir -p icons && python3 - <<'EOF'
import struct, zlib
def png(w, h, rgb):
    raw = b''.join(b'\x00' + bytes(rgb) * w for _ in range(h))
    def chunk(t, d):
        return struct.pack('>I', len(d)) + t + d + struct.pack('>I', zlib.crc32(t + d))
    return (b'\x89PNG\r\n\x1a\n'
            + chunk(b'IHDR', struct.pack('>IIBBBBB', w, h, 8, 2, 0, 0, 0))
            + chunk(b'IDAT', zlib.compress(raw))
            + chunk(b'IEND', b''))
for size in (192, 512):
    open(f'icons/icon-{size}.png', 'wb').write(png(size, size, (91, 70, 54)))
print('icons created')
EOF
```
Expected: `icons created` 출력, `icons/icon-192.png`와 `icons/icon-512.png` 생성됨

- [ ] **Step 5: 브라우저 확인**

Run: `cd /Users/shin/Desktop/ClaudeWork/book-app && python3 -m http.server 8080` (백그라운드)
브라우저에서 `http://localhost:8080` 열기.
Expected: 제목/부제/입력창/비활성화된 "도서 추천 받기" 버튼이 보임. (logic.js, app.js가 아직 없어 콘솔에 404가 뜨는 것은 정상 — Task 2~3에서 해결)

- [ ] **Step 6: Commit**

```bash
git add index.html style.css manifest.json icons/
git commit -m "feat: 앱 뼈대(HTML/CSS/manifest/아이콘) 추가"
```

---

### Task 2: 순수 로직 모듈 (logic.js) — TDD

**Files:**
- Create: `logic.js`, `tests/logic.test.js`

**Interfaces:**
- Produces (브라우저에서는 `window.BookLogic`, Node에서는 `require('../logic.js')`):
  - `normalizeWord(raw: string) -> string` — trim된 단어
  - `canAddWord(words: string[], raw: string) -> {ok: true, word: string} | {ok: false, reason: 'empty'|'duplicate'|'full'}`
  - `canRecommend(words: string[]) -> boolean` — 3~10개일 때 true
  - `extractJSON(text: string) -> any[]` — AI 응답 텍스트에서 첫 `[`~마지막 `]` 구간을 JSON.parse. 실패 시 throw
  - `validateBooks(data: any) -> Book[]` — `Book = {title, author, originalTitle, reason, summary}` (모두 string). title/author 없는 항목 제거, 최대 5개로 자름

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/logic.test.js`

```js
const assert = require('assert');
const L = require('../logic.js');

// normalizeWord
assert.strictEqual(L.normalizeWord('  여행  '), '여행');
assert.strictEqual(L.normalizeWord(''), '');

// canAddWord
assert.deepStrictEqual(L.canAddWord([], ' 고독 '), { ok: true, word: '고독' });
assert.deepStrictEqual(L.canAddWord([], '   '), { ok: false, reason: 'empty' });
assert.deepStrictEqual(L.canAddWord(['여행'], '여행'), { ok: false, reason: 'duplicate' });
const ten = ['a','b','c','d','e','f','g','h','i','j'];
assert.deepStrictEqual(L.canAddWord(ten, '새단어'), { ok: false, reason: 'full' });

// canRecommend: 3~10개만 true
assert.strictEqual(L.canRecommend(['a','b']), false);
assert.strictEqual(L.canRecommend(['a','b','c']), true);
assert.strictEqual(L.canRecommend(ten), true);

// extractJSON: 마크다운 펜스/잡담 섞인 응답에서 배열 추출
const messy = '추천 결과입니다!\n```json\n[{"title":"데미안","author":"헤르만 헤세"}]\n```\n감사합니다';
assert.deepStrictEqual(L.extractJSON(messy), [{ title: '데미안', author: '헤르만 헤세' }]);
assert.throws(() => L.extractJSON('죄송합니다, 추천할 수 없습니다.'));
assert.throws(() => L.extractJSON('[깨진 json'));

// validateBooks: title/author 필수, 최대 5개, 필드 정규화
const books = L.validateBooks([
  { title: ' 데미안 ', author: '헤르만 헤세', original_title: 'Demian', reason: '이유', summary: '요약' },
  { title: '', author: '무명' },          // title 없음 → 제거
  { author: '저자만' },                    // title 없음 → 제거
  { title: '한국책', author: '김작가' },   // 선택 필드 없음 → 빈 문자열
  { title: 'b3', author: 'a3' }, { title: 'b4', author: 'a4' },
  { title: 'b5', author: 'a5' }, { title: 'b6', author: 'a6' }
]);
assert.strictEqual(books.length, 5); // 유효 6개 중 5개로 잘림
assert.deepStrictEqual(books[0], { title: '데미안', author: '헤르만 헤세', originalTitle: 'Demian', reason: '이유', summary: '요약' });
assert.deepStrictEqual(books[1], { title: '한국책', author: '김작가', originalTitle: '', reason: '', summary: '' });
assert.deepStrictEqual(L.validateBooks('배열아님'), []);

console.log('✅ 모든 로직 테스트 통과');
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd /Users/shin/Desktop/ClaudeWork/book-app && node tests/logic.test.js`
Expected: FAIL — `Cannot find module '../logic.js'`

- [ ] **Step 3: logic.js 구현**

```js
(function (root) {
  'use strict';

  function normalizeWord(raw) {
    return (raw || '').trim();
  }

  function canAddWord(words, raw) {
    const w = normalizeWord(raw);
    if (!w) return { ok: false, reason: 'empty' };
    if (words.includes(w)) return { ok: false, reason: 'duplicate' };
    if (words.length >= 10) return { ok: false, reason: 'full' };
    return { ok: true, word: w };
  }

  function canRecommend(words) {
    return words.length >= 3 && words.length <= 10;
  }

  function extractJSON(text) {
    if (typeof text !== 'string') throw new Error('응답이 비어 있습니다');
    const start = text.indexOf('[');
    const end = text.lastIndexOf(']');
    if (start === -1 || end <= start) throw new Error('응답에서 JSON을 찾지 못했습니다');
    return JSON.parse(text.slice(start, end + 1));
  }

  function validateBooks(data) {
    if (!Array.isArray(data)) return [];
    return data
      .filter(b => b && typeof b.title === 'string' && b.title.trim()
                     && typeof b.author === 'string' && b.author.trim())
      .map(b => ({
        title: b.title.trim(),
        author: b.author.trim(),
        originalTitle: typeof b.original_title === 'string' ? b.original_title.trim() : '',
        reason: typeof b.reason === 'string' ? b.reason.trim() : '',
        summary: typeof b.summary === 'string' ? b.summary.trim() : ''
      }))
      .slice(0, 5);
  }

  const api = { normalizeWord, canAddWord, canRecommend, extractJSON, validateBooks };
  if (typeof module !== 'undefined' && module.exports) {
    module.exports = api;
  } else {
    root.BookLogic = api;
  }
})(this);
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node tests/logic.test.js`
Expected: `✅ 모든 로직 테스트 통과`

- [ ] **Step 5: Commit**

```bash
git add logic.js tests/logic.test.js
git commit -m "feat: 단어 검증·JSON 파싱 로직 모듈 추가 (테스트 포함)"
```

---

### Task 3: 단어 칩 입력 + 설정 화면 (app.js 1부)

**Files:**
- Create: `app.js`

**Interfaces:**
- Consumes: `window.BookLogic`의 `canAddWord`, `canRecommend` (Task 2), Task 1의 DOM id들
- Produces: 전역 상태 `words: string[]`, 함수 `getSetting(key, def)`, `setSetting(key, val)`, `showToast(msg)`, `showView(name)`, `escapeHTML(s)` — Task 4가 사용

- [ ] **Step 1: app.js 작성 (칩 입력 + 설정 + 유틸)**

```js
'use strict';
const L = window.BookLogic;

// ===== 유틸 =====
function escapeHTML(s) {
  return String(s).replace(/[&<>"']/g, c => (
    { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]
  ));
}

function getSetting(key, def) {
  const v = localStorage.getItem('bookapp_' + key);
  return v === null ? def : v;
}
function setSetting(key, val) {
  localStorage.setItem('bookapp_' + key, val);
}

function showToast(msg, duration = 2200) {
  const t = document.getElementById('toast');
  t.textContent = msg;
  t.classList.add('show');
  setTimeout(() => t.classList.remove('show'), duration);
}

function showView(name) {
  document.querySelectorAll('.view').forEach(v => v.classList.remove('active'));
  document.getElementById('view-' + name).classList.add('active');
}

document.querySelectorAll('.back-btn').forEach(btn => {
  btn.addEventListener('click', () => showView(btn.dataset.target));
});

// ===== 모델 목록 =====
const MODELS = [
  { id: 'openrouter/free', name: '자동 무료 모델 (기본)' },
  { id: 'google/gemma-4-31b-it:free', name: 'Gemma 4 31B (무료)' },
  { id: 'meta-llama/llama-4-scout:free', name: 'Llama 4 Scout (무료)' },
  { id: 'anthropic/claude-sonnet-4.6', name: 'Claude Sonnet 4.6 (유료·고품질)' }
];
const selectModel = document.getElementById('select-model');
MODELS.forEach(m => {
  const opt = document.createElement('option');
  opt.value = m.id;
  opt.textContent = m.name;
  selectModel.appendChild(opt);
});

// ===== 설정 화면 =====
document.getElementById('btn-settings-open').addEventListener('click', () => {
  document.getElementById('input-api-key').value = getSetting('api_key', '');
  selectModel.value = getSetting('model', MODELS[0].id);
  showView('settings');
});

document.getElementById('btn-save-settings').addEventListener('click', () => {
  setSetting('api_key', document.getElementById('input-api-key').value.trim());
  setSetting('model', selectModel.value);
  showToast('설정이 저장되었습니다.');
  showView('main');
});

// ===== 단어 칩 =====
let words = [];
const inputWord = document.getElementById('input-word');
const btnAddWord = document.getElementById('btn-add-word');
const btnRecommend = document.getElementById('btn-recommend');

function renderChips() {
  const box = document.getElementById('chips');
  box.innerHTML = words.map((w, i) =>
    `<span class="chip">${escapeHTML(w)}<button data-i="${i}" aria-label="삭제">✕</button></span>`
  ).join('');
  box.querySelectorAll('button').forEach(btn => {
    btn.addEventListener('click', () => {
      words.splice(Number(btn.dataset.i), 1);
      renderChips();
    });
  });
  document.getElementById('word-counter').textContent = `${words.length}/10`;
  btnRecommend.disabled = !L.canRecommend(words);
  const full = words.length >= 10;
  inputWord.disabled = full;
  btnAddWord.disabled = full;
  inputWord.placeholder = full ? '최대 10개까지 입력할 수 있어요' : '단어 입력 (예: 여행)';
}

function addWord() {
  const check = L.canAddWord(words, inputWord.value);
  if (!check.ok) {
    const msg = { empty: '단어를 입력해주세요.', duplicate: '이미 추가한 단어예요.', full: '최대 10개까지 입력할 수 있어요.' };
    showToast(msg[check.reason]);
    return;
  }
  words.push(check.word);
  inputWord.value = '';
  renderChips();
  inputWord.focus();
}

btnAddWord.addEventListener('click', addWord);
inputWord.addEventListener('keydown', e => {
  if (e.key === 'Enter') { e.preventDefault(); addWord(); }
});

renderChips();
```

- [ ] **Step 2: 브라우저 수동 검증**

`http://localhost:8080` 새로고침 후:
1. 단어 2개 추가 → 카운터 `2/10`, 추천 버튼 여전히 비활성
2. 3개째 추가 → 추천 버튼 활성화
3. 같은 단어 재입력 → "이미 추가한 단어예요." 토스트
4. 칩의 ✕ 클릭 → 삭제되고 카운터 감소, 2개가 되면 버튼 다시 비활성
5. 10개 채우기 → 입력창/추가 버튼 잠김, 플레이스홀더 문구 변경
6. ⚙️ → 키 입력 → 저장 → 토스트 확인 → 새로고침 후 ⚙️ 다시 열어 키 유지 확인
Expected: 전부 정상 동작, 콘솔 에러 없음

- [ ] **Step 3: Commit**

```bash
git add app.js
git commit -m "feat: 단어 칩 입력과 설정 화면 구현"
```

---

### Task 4: AI 추천 + 표지 + 카드 렌더링 (app.js 2부)

**Files:**
- Modify: `app.js` (파일 끝에 추가)

**Interfaces:**
- Consumes: Task 3의 `words`, `getSetting`, `showToast`, `showView`, `escapeHTML`, `btnRecommend`; Task 2의 `L.extractJSON`, `L.validateBooks`
- Produces: 없음 (최종 소비자)

- [ ] **Step 1: app.js 끝에 추천 흐름 코드 추가**

```js
// ===== AI 추천 =====
function buildPrompt(wordList) {
  return `당신은 도서 추천 전문가입니다.
사용자가 마음에 드는 단어들: ${wordList.join(', ')}

이 단어들이 담고 있는 정서와 주제에 어울리는, 실제로 출간된 도서를 4~5권 추천하세요(최소 3권).
한국 도서와 해외 도서(한국어 번역서 포함)를 모두 추천할 수 있습니다.
반드시 실존하는 책만 추천하고, 확실하지 않은 책은 제외하세요.

아래 JSON 배열 형식으로만 응답하세요. JSON 외의 텍스트는 절대 쓰지 마세요.
[
  {
    "title": "한국어판 제목(번역서면 번역 제목)",
    "author": "저자 이름",
    "original_title": "해외 도서의 원제 (한국 도서면 빈 문자열)",
    "reason": "입력 단어들과 이 책이 어떻게 연결되는지 2~3문장 (단어를 직접 언급)",
    "summary": "책 내용 요약 2~3문장"
  }
]`;
}

async function callOpenRouter(apiKey, model, prompt) {
  const response = await fetch('https://openrouter.ai/api/v1/chat/completions', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${apiKey}`,
      'Content-Type': 'application/json',
      'HTTP-Referer': location.href,
      'X-Title': 'Book Recommend App'
    },
    body: JSON.stringify({
      model,
      messages: [{ role: 'user', content: prompt }],
      max_tokens: 2000
    })
  });
  if (!response.ok) {
    const err = await response.json().catch(() => ({}));
    const error = new Error(err.error?.message || `API 오류 (${response.status})`);
    error.status = response.status;
    throw error;
  }
  const data = await response.json();
  return data.choices?.[0]?.message?.content || '';
}

// Google Books에서 표지 URL 조회 (실패 시 null)
async function fetchCover(book) {
  const queries = [
    `intitle:${book.title} inauthor:${book.author}`,
    book.title,
    book.originalTitle
  ].filter(Boolean);
  for (const q of queries) {
    try {
      const res = await fetch(`https://www.googleapis.com/books/v1/volumes?q=${encodeURIComponent(q)}&maxResults=5`);
      if (!res.ok) continue;
      const data = await res.json();
      const item = (data.items || []).find(it => it.volumeInfo?.imageLinks?.thumbnail);
      if (item) return item.volumeInfo.imageLinks.thumbnail.replace(/^http:/, 'https:');
    } catch (e) { /* 다음 쿼리 시도 */ }
  }
  return null;
}

function renderResults(books, covers) {
  const box = document.getElementById('results');
  box.innerHTML = books.map((b, i) => {
    const cover = covers[i]
      ? `<img src="${escapeHTML(covers[i])}" alt="${escapeHTML(b.title)} 표지">`
      : `<div class="cover-placeholder">${escapeHTML(b.title)}</div>`;
    const badge = covers[i] ? '' : '<span class="no-cover-badge">표지 정보 없음</span>';
    return `<div class="book-card">
      <div class="book-cover">${cover}</div>
      <div class="book-info">
        <h3>${escapeHTML(b.title)}</h3>
        <p class="book-author">${escapeHTML(b.author)}${b.originalTitle ? ' · ' + escapeHTML(b.originalTitle) : ''}</p>
        ${badge}
        <p class="book-reason"><strong>추천 이유</strong> ${escapeHTML(b.reason)}</p>
        <p class="book-summary">${escapeHTML(b.summary)}</p>
      </div>
    </div>`;
  }).join('');
}

function showError(msg) {
  const box = document.getElementById('error-box');
  box.textContent = msg;
  box.classList.remove('hidden');
}

async function recommend() {
  const apiKey = getSetting('api_key', '');
  if (!apiKey) {
    showToast('먼저 설정에서 API 키를 입력해주세요.');
    showView('settings');
    return;
  }
  const model = getSetting('model', MODELS[0].id);
  const prompt = buildPrompt(words);

  btnRecommend.disabled = true;
  document.getElementById('loading').classList.remove('hidden');
  document.getElementById('error-box').classList.add('hidden');
  document.getElementById('results').innerHTML = '';

  try {
    let books = null;
    // JSON 파싱 실패 시 1회 자동 재시도
    for (let attempt = 0; attempt < 2 && !books; attempt++) {
      const content = await callOpenRouter(apiKey, model, prompt);
      try {
        const parsed = L.extractJSON(content);
        const valid = L.validateBooks(parsed);
        if (valid.length > 0) books = valid;
      } catch (e) { /* 재시도 */ }
    }
    if (!books) {
      showError('AI 응답을 해석하지 못했습니다. 잠시 후 다시 시도하거나 설정에서 다른 모델을 선택해보세요.');
      return;
    }
    const covers = await Promise.all(books.map(fetchCover));
    renderResults(books, covers);
  } catch (e) {
    if (e.status === 401) showError('API 키가 올바르지 않습니다. 설정에서 키를 확인해주세요.');
    else if (e.status === 402) showError('OpenRouter 크레딧이 부족합니다. 무료 모델을 선택하거나 크레딧을 충전해주세요.');
    else if (e.status === 429) showError('요청이 너무 많습니다. 잠시 후 다시 시도해주세요.');
    else showError(`오류가 발생했습니다: ${e.message}`);
  } finally {
    document.getElementById('loading').classList.add('hidden');
    btnRecommend.disabled = !L.canRecommend(words);
  }
}

btnRecommend.addEventListener('click', recommend);
```

- [ ] **Step 2: 로직 테스트 회귀 확인**

Run: `node tests/logic.test.js`
Expected: `✅ 모든 로직 테스트 통과`

- [ ] **Step 3: 브라우저 실전 검증**

`http://localhost:8080` 새로고침 후:
1. API 키 없이(설정에서 키 지우고 저장) 단어 3개 → 추천 클릭 → 설정 화면으로 이동 + 토스트
2. 실제 OpenRouter 키 입력 후 "여행, 고독, 성장" 입력 → 추천 클릭
3. 스피너 표시 → 책 카드 3~5장 렌더링 확인 (표지, 제목, 저자, 추천 이유, 줄거리)
4. 표지 없는 책이 있으면 placeholder + "표지 정보 없음" 배지 확인
Expected: 전부 정상 동작 (실제 키가 없으면 이 단계는 사용자와 함께 확인)

- [ ] **Step 4: Commit**

```bash
git add app.js
git commit -m "feat: OpenRouter 도서 추천 + Google Books 표지 + 카드 렌더링"
```

---

### Task 5: 서비스 워커 + 최종 점검

**Files:**
- Create: `sw.js`

**Interfaces:**
- Consumes: Task 1에서 등록 스크립트가 이미 index.html에 존재 (`navigator.serviceWorker.register('sw.js')`)

- [ ] **Step 1: sw.js 작성** (앱 셸 캐시, API 요청은 네트워크 직행)

```js
const CACHE_NAME = 'bookapp-v1';
const ASSETS = [
  './',
  './index.html',
  './style.css',
  './logic.js',
  './app.js',
  './manifest.json',
  './icons/icon-192.png',
  './icons/icon-512.png'
];

self.addEventListener('install', e => {
  e.waitUntil(caches.open(CACHE_NAME).then(c => c.addAll(ASSETS)));
  self.skipWaiting();
});

self.addEventListener('activate', e => {
  e.waitUntil(
    caches.keys().then(keys =>
      Promise.all(keys.filter(k => k !== CACHE_NAME).map(k => caches.delete(k)))
    )
  );
  self.clients.claim();
});

self.addEventListener('fetch', e => {
  const url = new URL(e.request.url);
  // API 호출(openrouter, googleapis)은 캐시하지 않음
  if (url.origin !== location.origin) return;
  e.respondWith(
    caches.match(e.request).then(cached => cached || fetch(e.request))
  );
});
```

- [ ] **Step 2: 최종 수동 점검 체크리스트**

`http://localhost:8080`에서:
1. 콘솔에 404/에러 없음
2. Application 탭에서 서비스 워커 등록 확인
3. 단어 3개/10개 경계값 재확인
4. 모바일 뷰포트(개발자도구 iPhone 시뮬레이션)에서 레이아웃 확인
Expected: 전부 통과

- [ ] **Step 3: Commit**

```bash
git add sw.js
git commit -m "feat: 서비스 워커 추가 (PWA 오프라인 셸)"
```

---

### Task 6: GitHub Pages 배포

**Files:** 없음 (배포 작업)

- [ ] **Step 1: GitHub 저장소 생성 및 푸시**

```bash
cd /Users/shin/Desktop/ClaudeWork/book-app
gh repo create book-app --public --source=. --push
```
Expected: `dhshin6023/book-app` 저장소 생성 및 푸시 완료

- [ ] **Step 2: GitHub Pages 활성화**

```bash
gh api repos/dhshin6023/book-app/pages -X POST -f "source[branch]=main" -f "source[path]=/" 2>/dev/null \
  || gh api repos/dhshin6023/book-app/pages -X PUT -f "source[branch]=main" -f "source[path]=/"
```
Expected: Pages 활성화, 몇 분 후 `https://dhshin6023.github.io/book-app/` 접속 가능

- [ ] **Step 3: 배포 확인**

Run: `curl -s -o /dev/null -w "%{http_code}" https://dhshin6023.github.io/book-app/`
Expected: `200` (전파에 몇 분 걸릴 수 있음 — 404면 2~3분 후 재시도)

- [ ] **Step 4: 사용자 안내**

아이폰 사파리에서 접속 → 공유 → "홈 화면에 추가"로 PWA 설치 안내.
설정에서 OpenRouter API 키 입력 후 사용 시작.
